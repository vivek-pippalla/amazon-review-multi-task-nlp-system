flask
tensorflow
scikit-learn
transformers
spacy
rapidfuzz
ollama
pymysql
python-dotenv
sentencepiece
protobuf
# torch is installed separately in the Dockerfile from the CPU wheel index.
# For local dev (no Docker): pip install torch --index-url https://download.pytorch.org/whl/cpu

# commented libraries are not needed in docker. You can install them if you pull this from git
#numpy
#pandas
#matplotlib
#seaborn
#nltk
# Ensure spaCy English model is available
# You can install it with:
# python -m spacy download en_core_web_sm
